# Namecheap Dynamic DNS Configuration

# Comma-separated list of hostnames to update (e.g., "www,@" or "subdomain")
HOSTS=your-host-here

# Your domain name (e.g., "example.com")
DOMAIN=your-domain.com

# Path to file containing your Namecheap Dynamic DNS password
PASSWORD_FILE=./ddns-password.txt

# Cron schedule for updates (defaults to every hour)
# Format: "minute hour day month dayofweek"
# Examples:
#   "0 * * * *"     - Every hour
#   "*/15 * * * *"  - Every 15 minutes
#   "0 */6 * * *"   - Every 6 hours
CRON=0 * * * *
